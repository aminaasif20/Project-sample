# TODO

- [ ] Create/Update root `.gitignore` with best-practice ignores
- [ ] Ensure `README.md` contains `# Project-sample` (append if missing)
- [ ] `git add` and commit README, docker-compose.yml, backend/, frontend/
- [ ] Set/Update git remote `origin` to `https://github.com/aminaasif20/Project-sample.git`
- [ ] Push to `origin main` with upstream
- [ ] Verify with `git status` and `git log`

